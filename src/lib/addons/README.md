# Addons Directory

## Structure

├── README.m

│  
│   └──
    └── index.ts


2. Create an `index.t
4. Register it in `
See `/ADDONS.md` in the project root for comprehensi
## Guidelines
- Each addon should be self-contained
- Handle errors 
- T

The addon system is des

These core files handle addon integratio
- `/src/lib/addons.ts` - Core addon types and registry
- `/src/components/NodePalette.tsx` - No
Only modify these files if you're working on 
























